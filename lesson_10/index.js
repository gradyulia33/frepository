'use strict'
const arr1  = [20, 10, 11, 0, -2];
let ans1 = Math.max(20, 10, 11, 0, -2);
let ans2 = Math.min(20, 10, 11,  0, -2);
const average = arr => arr.reduce( ( p, c ) => p + c, 0 ) / arr.length;   
const res = average( [ 20, 10, 11, 0, -2] );   
console.log(obj);
const  obj = {max:ans1, min:ans2, avg:res};

